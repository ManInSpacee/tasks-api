services:
  db:
    image: postgres:17-alpine
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  tasks-app:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - db
volumes:
  postgres_data:
